package pertsev.VCS.Commit;

import java.nio.file.Path;

//Недоделка, возможно, рудимент
public class CommitFileHandler {
    private Path commitFile;

    public CommitFileHandler(Path commitFile) {
        this.commitFile = commitFile;
    }
}
